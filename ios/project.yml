name: MineracaoMG
options:
  bundleIdPrefix: com.mineracaomg
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true
  xcodeVersion: "15.4"

settings:
  base:
    SWIFT_VERSION: "5.9"
    PRODUCT_BUNDLE_IDENTIFIER: com.mineracaomg.MineracaoMG
    DEVELOPMENT_TEAM: ""
    IPHONEOS_DEPLOYMENT_TARGET: "17.0"

targets:
  MineracaoMG:
    type: application
    platform: iOS
    deploymentTarget: "17.0"
    sources:
      - path: MineracaoMG
    settings:
      base:
        INFOPLIST_FILE: MineracaoMG/Info.plist
        PRODUCT_BUNDLE_IDENTIFIER: com.mineracaomg.MineracaoMG
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
